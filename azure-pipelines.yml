# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java
trigger:
- main   # Ejecutar automáticamente al hacer push en la rama main

pool:
  vmImage: ubuntu-latest   # Usa una máquina virtual de Ubuntu

jobs:
- job: ParallelTesting
  displayName: "Ejecución paralela de ejemplo"
  strategy:
    parallel: 2   # 🔹 Ejecuta 2 trabajos en paralelo
  steps:
  - script: echo "Inicio del trabajo paralelo ${{ strategy.jobIndex }}"
    displayName: "Inicio del job"

  - script: echo "Simulando ejecución de pruebas en job ${{ strategy.jobIndex }}..."
    displayName: "Ejecutando pruebas ficticias"
  
  - script: echo "Finalizando trabajo ${{ strategy.jobIndex }}"
    displayName: "Finalización del job"